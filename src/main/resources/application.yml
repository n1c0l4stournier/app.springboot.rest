spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://${DB_URL:localhost}/db_rest
    username: ${DB_USER:debian-sys-maint}
    password: ${DB_PASS:3Xf6hcpbyySgX9Tu}
    driver-class-name: com.mysql.jdbc.Driver
server:
  port: 8181
